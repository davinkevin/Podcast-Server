apiVersion: v1
kind: Service
metadata:
  name: ui
  labels:
    app: podcast-server
    module: frontend
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: podcast-server
    module: frontend
    serving: "true"
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ui
spec:
  selector:
    matchLabels:
      app: podcast-server
      module: frontend
      serving: "true"
  template:
    metadata:
      labels:
        app: podcast-server
        module: frontend
        serving: "true"
    spec:
      containers:
        - image: podcastserver/ui:latest
          imagePullPolicy: IfNotPresent
          name: ui
          readinessProbe:
            httpGet:
              path: /
              port: http
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
